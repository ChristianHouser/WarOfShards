# War of Shards

A match-3 RPG, styled after Empires & Puzzles. Static HTML/CSS/JS, hosted via GitHub Pages.

Currently just the character roster/stats page (`index.html` + `characters.js`) — no board or battle logic yet.
